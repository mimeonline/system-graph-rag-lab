# Bug 0001 Story im QA Status ohne QA Evidenz

## Titel
1. Story `E1-S1` steht auf `qa`, aber es liegt kein dokumentierter QA-Testlauf in QA-Artefakten vor.

## Schweregrad
1. Medium.

## Repro Steps
1. Datei `backlog/progress.md` lesen und Story-Status fuer `E1-S1` pruefen.
2. Datei `docs/qa/test-matrix.md` auf Eintrag `E1-S1` mit markiertem Pass oder Fail pruefen.
3. Datei `docs/qa/verdict.md` auf dokumentierten Story-Testlauf pruefen.
4. Ergebnis vergleichen: Story in `qa`, aber keine ausgefuehrte QA-Evidenz.

## Expected vs Actual
1. Expected: Jede Story mit Status `qa` hat reproduzierbaren QA-Lauf mit Pass oder Fail und Evidenzpfad.
2. Actual: Story `E1-S1` ist auf `qa`, aber der QA-Lauf ist im Bootstrap-Stand noch nicht ausgefuehrt.

## Logs oder Screenshots Referenzen
1. `backlog/progress.md`
2. `docs/qa/test-matrix.md`
3. `docs/qa/verdict.md`

## Workaround
1. Sofortigen Story-QA-Lauf fuer `E1-S1` ausfuehren und Matrix plus Verdict aktualisieren.
