# Bug 0003 Story-Testkommando fuer E1-S6 ist als Scope-Kommando ungenau

## Status
1. Open am 2026-02-26 im QA-Run `RUN_MODE=review` fuer `E1-S6`.
2. Recheck am 2026-02-26 bestaetigt: `pnpm --dir apps/web test -- src/features/seed-data/local-seed-reset.test.ts` bleibt breit und fuehrt 7 Testdateien aus.
3. Closed am 2026-02-26, weil Story und Dev-Handoff fuer E1-S6 auf das praezise Scope-Kommando `pnpm --dir apps/web exec vitest run src/features/seed-data/local-seed-reset.test.ts` normiert sind.

## Titel
1. Das in Story und Dev-Handoff genannte Kommando `pnpm --dir apps/web test -- src/features/seed-data/local-seed-reset.test.ts` fuehrt die Gesamtsuite statt nur des Ziel-Testfiles aus.

## Schweregrad
1. Low.

## Repro Steps
1. Kommando ausfuehren: `pnpm --dir apps/web test -- src/features/seed-data/local-seed-reset.test.ts`.
2. Testausgabe pruefen und Anzahl der Testdateien vergleichen.
3. Vergleichskommando ausfuehren: `pnpm --dir apps/web exec vitest run src/features/seed-data/local-seed-reset.test.ts`.

## Expected vs Actual
1. Expected: Das Story-Kommando prueft nur `src/features/seed-data/local-seed-reset.test.ts`.
2. Actual: Das Story-Kommando fuehrt alle 7 Testdateien aus.
3. Actual Vergleich: Der direkte Vitest-Run prueft exakt 1 Testdatei mit 6 Tests.

## Logs oder Screenshots Referenzen
1. QA-Lauf 2026-02-26 in `docs/qa/verdict.md`.
2. Story-Testnotes in `backlog/stories/e1-s6-neo4j-lokal-seed-reset.md`.
3. Dev-Handoff `docs/handoff/dev-to-qa.md`.

## Workaround
1. Fuer story-spezifische Evidenz den direkten Aufruf nutzen: `pnpm --dir apps/web exec vitest run src/features/seed-data/local-seed-reset.test.ts`.
